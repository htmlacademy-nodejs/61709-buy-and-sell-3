extends layout.pug
include partials/offers-list

block content
  -var OFFER_LIST_TYPE = `search`
  main.page-content
    section.search-results
      h1.visually-hidden Результаты поиска
      .search-results__wrapper
        if offers.length === 0
          .search-results__message
            p
              | Не найдено
              br
              | ни одной публикации
        else
          p.search-results__label
            | Найдено
            span.js-results= ` ${offers.length} публикации(ия)`
          +offers-list(offers, OFFER_LIST_TYPE)
      section.tickets-list
        h2.visually-hidden Самые новые предложения
        .tickets-list__wrapper
          .tickets-list__header
            p.tickets-list__title Самое свежее
            a.tickets-list__link(href="#") Еще 25
          +offers-list(mostDiscussedOffers)
